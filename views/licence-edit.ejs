<%- include('_header'); -%>

<nav class="breadcrumb">
  <ul>
    <li><a href="/dashboard">Home</a></li>
    <li><a href="/licences">Licences</a></li>
    <% if (isCreate) { %>
      <li class="is-active"><a href="#" aria-current="page">New Licence</a></li>
    <% } else { %>
      <li><a href="/licences/<%= organization.LicenceID %>">Licence #<%= organization.LicenceID %></a></li>
      <li class="is-active"><a href="#" aria-current="page">Update</a></li>
    <% } %>
  </ul>
</nav>

<h1 class="title is-1">
  <%= (isCreate ? "Create a New Licence" : "Update an Existing Licence") %>
</h1>

<form id="form--licence">
  <div class="columns is-multiline has-margin-b-0">
    <div class="column">
      <div class="field">
        <label class="label" for="licence--licenceID">Licence ID</label>
        <div class="control">
          <input class="input has-background-light has-cursor-not-allowed" id="licence--licenceID" name="licenceID" type="text" value="<%= licence.LicenceID %>" placeholder="(New Licence Number)" readonly required />
        </div>
      </div>
    </div>
    <div class="column">
      <div class="field">
        <label class="label" for="licence--externalLicenceNumber"><%= (((config.licences || {}).externalLicenceNumber || {}).fieldLabel || "External Licence Number") %></label>
        <div class="control">
          <input class="input" id="licence--externalLicenceNumber" name="externalLicenceNumber" type="text" value="<%= licence.ExternalLicenceNumber %>" maxlength="20" />
        </div>
      </div>
    </div>

    <div class="column is-full">
      <input id="licence--organizationID" name="organizationID" type="hidden" value="<%= organization.OrganizationID %>" />
      <label class="label" for="licence--organization">Organization</label>
      <div class="field has-addons">
        <div class="control is-expanded">
          <input class="input has-background-light has-cursor-not-allowed" id="licence--organizationName" type="text" value="<%= organization.OrganizationName %>" readonly required />
        </div>
        <div class="control">
          <button class="button is-organization-lookup-button" type="button">
            <span class="icon"><i class="fas fa-search"></i></span>
            <span>Lookup</span>
          </button>
        </div>
      </div>
    </div>

    <div class="column is-half">
      <div class="field">
        <label class="label" for="licence--applicationDateString">Application Date</label>
        <div class="control">
          <input class="input" id="licence--applicationDateString" name="applicationDateString" type="date" value="<%= licence.ApplicationDateString %>" required />
        </div>
      </div>
    </div>
    <div class="column is-half">
      <div class="field">
        <label class="label" for="licence--licenceTypeKey">Licence Type</label>
        <div class="control">
          <div class="select is-fullwidth">
            <select id="licence--licenceTypeKey" name="licenceTypeKey" required>
              <option value="">(Select a Licence Type)</option>
              <%
                const licenceTypes = config.licenceTypes || [];
                for (let licenceTypeIndex = 0; licenceTypeIndex < licenceTypes.length; licenceTypeIndex += 1) {
                  const licenceType = licenceTypes[licenceTypeIndex];

                  if (!licenceType.isActive && licence.LicenceTypeKey !== licenceType.licenceTypeKey) {
                    continue;
                  }
              %>
                  <option value="<%= licenceType.licenceTypeKey %>" <%= (licence.LicenceTypeKey === licenceType.licenceTypeKey ? " selected" : "") %>>
                    <%= licenceType.licenceType %>
                  </option>
              <%
                }
              %>
            </select>
          </div>
        </div>
      </div>
    </div>

    <div class="column is-full">
      <div class="field">
        <label class="label" for="licence--location">Location</label>
        <input class="input" id="licence--location" name="location" type="text" value="<%= licence.Location %>" maxlength="100" />
      </div>
    </div>

    <div class="column is-one-quarter">
      <div class="field">
        <label class="label" for="startDateString">Start Date</label>
        <div class="control has-icons-left">
          <input class="input" id="licence--startDateString" name="startDateString" type="date" value="<%= licence.StartDateString %>" min="<%= licence.ApplicationDateString %>" max="<%= licence.EndDateString %>" required />
          <span class="icon is-left">
            <i class="fas fa-play"></i>
          </span>
        </div>
      </div>
    </div>
    <div class="column is-one-quarter">
      <div class="field">
        <label class="label" for="endDateString">End Date</label>
        <div class="control has-icons-left">
          <input class="input" id="licence--endDateString" name="endDateString" type="date" value="<%= licence.EndDateString %>" min="<%= licence.StartDateString %>" required />
          <span class="icon is-left">
            <i class="fas fa-stop"></i>
          </span>
        </div>
      </div>
    </div>
    <div class="column is-one-quarter">
      <div class="field">
        <label class="label" for="licence--startTimeString">Start Time</label>
        <div class="control has-icons-left">
          <input class="input" id="licence--startTimeString" name="startTimeString" type="time" value="<%= licence.StartTimeString %>" step="300" required />
          <span class="icon is-left">
            <i class="fas fa-play"></i>
          </span>
        </div>
      </div>
    </div>
    <div class="column is-one-quarter">
      <div class="field">
        <label class="label" for="licence--endTimeString">End Time</label>
        <div class="control has-icons-left">
          <input class="input" id="licence--endTimeString" name="endTimeString" type="time" value="<%= licence.EndTimeString %>" step="300" required />
          <span class="icon is-left">
            <i class="fas fa-stop"></i>
          </span>
        </div>
      </div>
    </div>

    <div class="column is-full">
      <div class="field">
        <label class="label" for="licence--licenceDetails">Licence Details</label>
        <div class="control">
          <textarea class="textarea" id="licence--licenceDetails" name="licenceDetails" rows="1"><%= licence.LicenceDetails %></textarea>
        </div>
      </div>
    </div>
    <div class="column is-full">
      <div class="field">
        <label class="label" for="licence--termsConditions">Terms and Conditions</label>
        <div class="control">
          <textarea class="textarea" id="licence--termsConditions" name="termsConditions" rows="2"><%= licence.TermsConditions %></textarea>
        </div>
      </div>
    </div>
  </div>

  <h2 class="title is-4">Events</h2>

  <div class="columns">
    <div class="column is-one-third">
      <div class="box has-background-light">
        <label class="label" for="eventCalc--eventCount">Number of Events</label>
        <div class="field has-addons">
          <div class="control">
            <input class="input has-text-right" id="eventCalc--eventCount" type="number" min="1" max="366" step="1" value="1" />
          </div>
          <div class="control">
            <span class="button is-static">event(s)</span>
          </div>
        </div>

        <label class="label" for="eventCalc--dayInterval">Interval</label>
        <div class="field has-addons">
          <div class="control">
            <input class="input has-text-right" id="eventCalc--dayInterval" type="number" min="1" max="90" step="1" value="7" />
          </div>
          <div class="control">
            <span class="button is-static">day(s)</span>
          </div>
        </div>

        <button class="button is-small is-fullwidth is-calculate-events-button" type="button">Calculate and Add</button>
      </div>
    </div>
    <div class="column">
      <div id="container--events"></div>
      <button class="button is-small is-fullwidth is-add-event-button has-margin-top-10" type="button">Add an Event Date</button>
    </div>
  </div>

  <%
    for (let licenceTypeIndex = 0; licenceTypeIndex < licenceTypes.length; licenceTypeIndex += 1) {
      const licenceType = licenceTypes[licenceTypeIndex];
  %>
    <fieldset class="container-licenceTypeFields <%= (licence.LicenceTypeKey === licenceType.licenceTypeKey ? "" : " is-hidden") %>" id="container-licenceTypeFields--<%= licenceType.licenceTypeKey %>" <%= (licence.licenceTypeKey === licenceType.licenceTypeKey ? "" : " disabled") %>>
      <legend class="title is-4"><%= licenceType.licenceType %></legend>
      <%
        const licenceFields = licenceType.licenceFields || [];
        let fieldKeys = "";

        for (let fieldIndex = 0; fieldIndex < licenceFields.length; fieldIndex += 1) {
          const licenceField = licenceFields[fieldIndex];

          const inputName = licenceType.licenceTypeKey + "-" + licenceField.fieldKey;

          fieldKeys += "," + inputName;

          const inputAttributes = licenceField.inputAttributes || {};

          if (!inputAttributes.type) {
            inputAttributes.type = "text";
          }

          let inputAttributesString = "";

          for (let attributeName in inputAttributes) {
            inputAttributesString += " " + attributeName + "=\"" + inputAttributes[attributeName] + "\"";
          }
      %>
        <div class="field">
          <label class="label" for="licence--<%=inputName %>"><%= licenceField.fieldLabel || licenceField.fieldKey %></label>
          <div class="control">
            <input class="input" id="licence--<%= inputName %>" name="<%= inputName %>" <%- inputAttributesString %> />
          </div>
        </div>
      <%
        }
      %>
      <input name="fieldKeys" type="hidden" value="<%= fieldKeys %>" />
    </fieldset>
  <%
    }
  %>

  <div class="level">
    <div class="level-left" id="container--form-message">
    </div>
    <div class="level-right">
      <div class="buttons">
        <a class="button" href="<%= (isCreate ? "/licences" : "/licences/" + licence.LicenceID) %>">
          Cancel
        </a>
        <button class="button is-success" type="submit">
          <span class="icon">
            <i class="fas fa-save"></i>
          </span>
          <span><%= (isCreate ? "Create New Licence and Continue" : "Update Licence") %></span>
        </button>
      </div>
    </div>
  </div>
</form>

<div class="modal is-organization-lookup-modal">
  <div class="modal-background"></div>
  <form>
    <div class="modal-card">
      <header class="modal-card-head">
        <h3 class="modal-card-title">Select an Organization</h3>
        <button class="delete is-cancel-button" aria-label="close" type="button"></button>
      </header>
      <section class="modal-card-body">
        <div class="control has-icons-left">
          <input class="input" id="organizationLookup--searchStr" type="text" placeholder="Filter Organizations" disabled />
          <span class="icon is-left">
            <i class="fas fa-filter"></i>
          </span>
        </div>
        <div class="has-margin-top-20" id="container--organizationLookup">
          <p class="has-text-centered has-text-grey-lighter">
            <i class="fas fa-3x fa-circle-notch fa-spin"></i><br />
            Loading organizations...
          </p>
        </div>
      </section>
      <footer class="modal-card-foot justify-right">
        <button class="button is-cancel-button" type="button">Cancel</button>
      </footer>
    </div>
  </form>
</div>

<%- include('_footerA'); -%>

<script>
  const config_licenceTypes = <%- JSON.stringify(config.licenceTypes || []) %>;
</script>

<script src="/javascripts/licence-edit.min.js"></script>

<%- include('_footerB'); -%>
