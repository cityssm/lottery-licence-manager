import type { User } from '../../types/recordTypes.js';
export default function deleteOrganizationBankRecord(organizationID: number | string, recordIndex: number | string, requestUser: User): boolean;
