import type * as expressSession from "express-session";
export declare const deleteOrganizationRemark: (organizationID: number, remarkIndex: number, requestSession: expressSession.Session) => boolean;
